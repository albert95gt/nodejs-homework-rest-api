const path = require('path');
const TEMP_DIR = path.join(__dirname, '../', 'temp');
const AVATARS = 'avatars';
const BASE_URL = 'https://mighty-spire-59228.herokuapp.com/api';

module.exports = {
  TEMP_DIR,
  AVATARS,
  BASE_URL,
};
